{
  "name": "edge-api",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "globals": "^17.4.0",
    "vitest": "^4.0.18",
    "wrangler": "^3.0.0"
  }
}
